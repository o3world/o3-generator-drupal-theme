// .storybook/YourTheme.js

import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: '<%= pascalTitle %> Design System',
  brandUrl: 'https://example.com',
});
